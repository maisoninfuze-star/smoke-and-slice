node_modules/
.next/
out/
build/

# Secrets — never commit
.env
.env.local
.env*.local

# Local database
prisma/dev.db
prisma/dev.db-journal
*.db

.DS_Store
*.pem
npm-debug.log*
.vercel
next-env.d.ts

# --- media that must not be published ---
# Third-party content: these two clips are a different client's wedding
# (a named welcome sign and a banquet hall). Nothing to do with this
# restaurant and not ours to publish.
brand/client-media/originals/video-1.mp4
brand/client-media/originals/video-2.mp4
# Raw phone drops — superseded by the processed assets in public/media
brand/client-media/whatsapp-raw/
# Full-quality intermediate master, 18MB and regenerated by the fal script
brand/masters/

# Build artifacts
*.tsbuildinfo
